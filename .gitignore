node_modules/
# dist/ はビルド生成物。追跡するのは「他に原本が無いもの」だけにする。
# ★ scripts/ や リポジトリ直下に原本があるファイル (relay の ps1/bat/loader、
#   仕様書、CSV 雛形など) は dist にコピーされるが **追跡しない**。
#   両方を追跡すると同じ内容が git に二重に入り、1 箇所の修正で毎回 2 ファイル
#   分の差分が出るため。配布物一式が要るときは `npm run build` で dist に揃う。
dist/*
!dist/index.html
!dist/install.html
!dist/install-loader.html
!dist/bookmarklet.txt
!dist/mikke.bundle.js
!dist/version.txt
!dist/relay-version.txt
!dist/README.md
*.log
.DS_Store
.vscode/

# relay 自己更新 (updater.bat) が生成するバックアップ / ステージング ファイル。
*.bak
*.new
*.orig
*~

# relay の個人設定 (テンプレートは .example として残す)
scripts/mikke-relay.env
# 検査ツール API アダプタの実装版 (委託先環境で作成。API 仕様・URL を公開リポに置かない)
scripts/mikke-scanner-adapter.ps1
dist/mikke-scanner-adapter.ps1
.claude/

# 開発専用の指示書。pull 同期対象外 (リポジトリで配布しない / 各自ローカル管理)。
CLAUDE.md
